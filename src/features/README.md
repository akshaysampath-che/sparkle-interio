# Features

Put domain-oriented modules here (e.g. `auth`, `catalog`, `checkout`). Each feature should own its components, hooks, and server/client code, and expose a small public API from an `index.ts`.

