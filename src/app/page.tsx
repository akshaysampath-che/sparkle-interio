/**
 * Placeholder — hero and homepage sections will be added later.
 * Transparent nav is active on `/` (see transparentNavRoutes).
 */
export default function Home() {
  return <div className="min-h-dvh" aria-hidden />
}
